# TASK039 independent controller review

Reviewer: contracts agent, 2026-09-21. Scope: uncommitted TASK039 changes to `src/embodied_jepa/waypoint_planning.py`, its focused tests and `docs/experiments/apple_control_commitment_v1.md`, against the preceding controller. The reviewer did not implement this controller change; they authored the sensor backend and earlier matched branch diagnostic. This is not external maintainer approval. Per-attempt supervisor implementation is reviewed separately by the simulation agent and coordinator.

**No blocking controller findings remain.** Commitment length defaults to one and is validated as an integer within the prediction horizon. With four, the root command plus at most three further commands share an original scored plan. Every call still requires a fresh synchronized observation and mandatory acknowledgement; observed image progress and dwell update once. Waypoint advance discards the cache before any cached projection. The task scorer does not enter this decision.

The next cached command alone passes fresh K1/H1 projection. Infeasibility or any non-array-equal projection change discards it and falls through to exactly one ordinary full search on the same observation. Projection errors are not hidden. Actual execution rejection remains terminal. Cache acknowledgement advances its offset only after acceptance; a changed applied action clears the cache. H16 pending/warm state remains separate from the short commitment, preserving the original shift operation.

Accepted cached decisions do not call model prediction or consume planner RNG in any of the three modes. They retain the originating plan step, goal/hash, timestamp, horizon, candidate index/round and explicitly labeled assigned search cost. Full search records retain requested and projected sequences; cached records retain the corresponding offset and fresh projection evidence. Review requests for explicit cache-clear reasons and remaining-command counts were implemented, including waypoint advancement, completed commitment, applied-action changes and rejection. The inherited dynamics-shuffle cost is labeled an assigned original search cost, not a new forecast of the cached command.

Independent checks:

- `PYTHONPATH=src .venv/bin/pytest -q tests/test_waypoint_planning.py`: **24 passed in 0.04 seconds**. Coverage includes all three modes, cache offsets, no cached RNG/prediction calls, H16 warm/pending shape, exact revalidation, single fallback/single dwell update, advance, applied-action changes and terminal rejection.
- A separate read-only synthetic comparator loaded the preserved original controller from `outputs/apple-control-development-v2/source` alongside the changed controller. Across **144 transitions** (three modes, four seeds, twelve steps, including waypoint advancement), default-mode actions, selected cost/source/round, goal progress, RNG state and H16 warm arrays matched exactly. This used a toy opaque model and no physics.
- Ruff check and format check on both controller files, plus `git diff --check`, passed.

The prospective hypothesis and limits are appropriate: reduced replanning might permit useful later commands to execute, but TASK038 did not establish it as the unique stall cause or prove that four is optimal. The fixed checkpoint, goals, physics, guards and ablation mechanisms remain unchanged. New per-attempt resource caps differ from the historical comparison and are explicitly disclosed; shortened/incomplete attempts remain in the denominator. Fewer model calls or improved image distance alone will not establish grasp, placement, causal benefit or MVP completion. No training, actual model inference or simulator run was performed for this review. The authorized bounded control experiment remains unverified until executed.

## Additional evaluator read-only review

The contracts reviewer also inspected optional cap propagation, source snapshot identity, default behavior, timeout classification and the revised supervisor loop. The parent passes a per-attempt cap only when requested; `None` preserves total-budget-only operation. Parent timing starts before spawning, while the child entry clock includes its imports and startup. The supervisor checks elapsed wall/monotonic time before accepting an observed exit, so a late zero exit cannot count as an on-time completion. Full allocations and globally shortened allocations remain distinguished, including soft timeout records. The unchanged source, asset, checkpoint, data and generated-waypoint integrity checks still bind execution to the captured runtime. No additional blocking issue was found in these paths.

At the time of this supplementary read-only review, the known preparation-readiness correction was **pending**: launch readiness must require supervisor status `exited` as well as exit code zero and a resolved plan file. A late zero-exit preparation must not launch attempts. Final evaluator regression tests, including this condition and full-slot versus shortened soft-timeout classification, were also pending; the simulation agent owns those fixes/tests and the coordinator will verify the final patch and full suite. This paragraph is not a claim that those pending checks passed.

## Final evaluator closure

The coordinator reviewed the final evaluator patch and confirmed preparation requires both `status == exited` and zero return code. Soft and hard limits preserve explicit attempt/global scope and all six planned records. The final combined controller/evaluator command passed **58 tests in 0.13 seconds**; Ruff check/format passed across all 81 Python files. The full optional/model/rendering suite passed **488 tests in 11.27 seconds** before the final evaluator-only timeout correction; the final focused run covers that correction and its regressions. No blocking issue remains for the single preregistered comparison. This review does not establish its physical outcome.
