# TASK037 independent training review

Reviewer: contracts agent, 2026-09-21. Scope: task-specific uncommitted changes in `jepa-apple-horizon` against starting HEAD `5138e808751cf8164c9b64a2dad27113d76b6f8e`, covering generic training sampling groups, the bounded `branches_h16_v1` supervisor profile, focused tests and `apple_branch_training_h16_v1.md`. Review is independent of this implementation; the reviewer authored the earlier sensor model, original supervisor and matched diagnostic. This is an engineering/research review, not external maintainer approval.

Design review: equal group quotas directly address the change from nine H8 windows to one H16 window per branch. Membership is an explicit exact partition of each sealed TRAIN/VAL split, validated before payload decoding, rather than a task-specific condition hidden in the model. Within-group cumulative integer offsets give uniform window sampling with replacement, and the absent-group path preserves its previous RNG sequence. Parent/session membership stays unchanged. Normalization still sees every TRAIN transition through the existing streaming path and never sees VAL.

The experiment jointly changes rollout horizon and sample weighting, including validation weighting; improvements cannot be attributed to horizon alone or compared as though raw validation MSE used the old distribution. The separate matched endpoint diagnostic retains its cohort and thresholds. H16 raw validation MSE selects a checkpoint using recursive error averaged over the rollout, while the causal diagnostic evaluates the final endpoint. Three VAL parent sessions remain a substantive uncertainty limitation.

Final review: **no blocking findings**. The supervisor registers exact group-file bytes plus canonical group hash before optimization; the child embeds groups/hash/RNG in report/checkpoints; the parent checks immutable files, H16 selection and the child sampling metadata afterward. Unknown, incomplete, duplicate, empty, cross-split and TEST group membership is rejected. The new profile leaves both older profiles unchanged.

Independent focused command: `PYTHONPATH=src .venv/bin/pytest -q tests/test_training.py tests/test_apple_training.py` — **43 passed in 3.05 seconds**. Coverage includes actual short H16 sensor training/checkpoint provenance, deterministic validation cohorts, quota/window weighting, exact default RNG behavior, invalid groups rejected before decode, cross-split parent rejection, CLI profiles and group-file mutation invalidation. Ruff check, formatting check and `git diff --check` passed. An initial existing test expected the prior `no windows` phrase; the compatible message was restored and the complete focused suite rerun successfully.

Metadata-only corpus verification found TRAIN H16 window counts 12,735 original / 384 branch, and VAL 1,457 / 144. Under unbalanced H16 sampling branch exposure would be approximately 2.93% and 8.99%, respectively. Explicit equal quotas raise both to 50%, as declared. No payload or model diagnostic was needed for this count.

Task readiness: implementation, bounded prospective protocol, split/provenance checks and focused regressions are met. The full 3,000-update attempt and unchanged matched causal gate remain **unverified until executed**. No production training, physics or diagnostic experiment was run by this review. No learned-control success or permission to bypass a failed gate is implied.

## Post-execution evidence review

Coordinator record of the contracts agent's subsequent independent saved-evidence audit: all eight diagnostic summaries and their bootstrap intervals were recomputed from saved pairs. Checkpoint, split, action, normalization membership (410 TRAIN episodes), sampling-group files, configuration and source hashes were checked. No blocking discrepancy was found. The earlier unverified-experiment statement describes the pre-execution review; training and the matched diagnostic have now completed.

The unchanged primary VAL H16 gate passes: 89.40% action assignment and 81.41% endpoint error reduction, with a 76.70–85.32% root-bootstrap interval for reduction. This remains evidence from 18 correlated roots across three VAL parents, not a manipulation-success result. The selected update-3000 checkpoint and full evidence are linked in `apple_branch_h16_diagnostics_results_v1.md` and its compact manifest.

The coordinator also reviewed the final source diff and the resumed control report. The bounded physical comparison produced one completed learned failure, one persistence timeout and four unstarted attempts; no grasp or placement was achieved. Its 1,846 acknowledged commands and immutable inputs were audited by the simulation agent. No complete six-episode performance estimate or learned control advantage is supported. The first 574 resumed learned commands duplicate the interrupted run, so they are not independent observations. TASK-033 and final acceptance remain open.
