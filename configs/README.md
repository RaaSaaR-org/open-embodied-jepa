# configs

Status: implementation planned.

Design templates only; required values remain null until measured and validated; future loader must reject unresolved values.

MissionControl: TASK-006, TASK-022. Run `mc show TASK-NNN` from the project root for acceptance criteria.
